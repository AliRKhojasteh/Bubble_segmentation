# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
Pillow>=9.0.0
matplotlib>=3.7.0
opencv-python>=4.7.0
scipy>=1.10.0

# SAM2 (install from source)
# pip install git+https://github.com/facebookresearch/sam2.git

# For interactive editor (local Jupyter only, not needed on Colab)
ipympl>=0.9.0
